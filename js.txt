function orderNow(productName) {
  let number = "923001234567"; // apna WhatsApp number
  let message = `Hello, I want to order: ${productName}`;
  let url = `https://wa.me/${number}?text=${encodeURIComponent(message)}`;
  window.open(url, "_blank");
}
